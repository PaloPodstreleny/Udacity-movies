<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/parent_layout"
    style="@style/Dashboard.ListItemLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/movie_photo"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:foreground="@drawable/background_shape_image"
        app:layout_constraintDimensionRatio="H,5:7.5"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout_height="250dp" />

    <TextView
        android:id="@+id/rating_tv"
        style="@style/Dashboard.RatingText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="@id/movie_photo"
        app:layout_constraintTop_toTopOf="@id/movie_photo"
        tools:text="9.7" />


    <TextView
        android:id="@+id/movie_title"
        style="@style/Dashboard.TitleText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@id/movie_photo"
        app:layout_constraintLeft_toLeftOf="@id/movie_photo"
        app:layout_constraintRight_toLeftOf="@id/movie_photo"
        tools:text="Title of movie" />


</android.support.constraint.ConstraintLayout>